<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModel="clr-namespace:CharterSampleApp.ViewModels"
             x:Class="CharterSampleApp.Views.HomePage">
    <ContentPage.BindingContext>
        <viewModel:HomeViewModel />
    </ContentPage.BindingContext>
    <Shell.TitleView>
        <Label Style="{StaticResource TitleBarStyle}">
            <Label.FormattedText>
                <FormattedString>
                    <Span Text="Spectrum" />
                    <Span Text="" />
                    <Span Text="&#xf0da;" FontFamily="FaIcons" />
                </FormattedString>
            </Label.FormattedText>
        </Label>

    </Shell.TitleView>
    <ContentPage.ToolbarItems>
        <ToolbarItem 
            Order="Primary" Command="{Binding GoToSettingsCommand}"
                     Priority="0" >
        <ToolbarItem.IconImageSource>
            <FontImageSource FontFamily="FaIcons" Size="Small"
                             Glyph="&#xf013;"/>
        </ToolbarItem.IconImageSource>
            </ToolbarItem>
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <StackLayout>
            <Label Text="Welcome to Xamarin.Forms!"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>